---
description: Build context package with scout and context-builder
---

先让 xdd-scout 只读定位入口和风险，再让 xdd-context-builder 生成上下文包。

上下文包必须包含：相关文件、AGENTS.md 约束、正向假设、兜底风险、推荐 planner/worker/reviewer 任务。

$@
