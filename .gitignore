.opencode/wireflow
.opencode/tmp/
.opencode/node_modules/
.opencode/package.json
.opencode/package-lock.json
.opencode/bun.lock
**__pycache__/

# Demo project (excluded from framework repo)
demo/

# 用户模型配置 (旧 install 软链反向写入, 非 framework 内容)
settings.json

# 回归测试运行时产物（trial 目录、m2cc 输出、报告、cron 日志）
regression/runs/
regression/reports/

# workflow 模型配置（含 API key，不入库）
workflow/models.yaml

# xdd 设计产物（workflow/ 当 demo 项目跑 xdd 产出，单轮工作记录不入库）
workflow/.xdd/
workflow/log/
