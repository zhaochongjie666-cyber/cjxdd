# Design — {项目名} 项目级总决策 ({date})

> 【项目层】收敛自发散笔记。只写**跨业务线的全局决策**（项目要什么、全局怎么定）。
> 下游 xdd-spec 只读这份。**业务线级细节**（端点/规则/架构）别写这里，落到 `spec/BXX/` + `architecture/BXX/`。

## Selected（选定方案）

本轮项目到底做什么。1-3 句话说清，让下游能照着翻译成规则。**写项目总决策**（如：用 PostgreSQL / 错误码 GS-BXX-NNNN / JWT 鉴权），单业务线的具体功能设计见 `spec/BXX/`。

{方案描述}

## Alternatives（被否方案）

| 方案 | 为什么没选 |
|------|-----------|
| {方案 A} | {一句理由} |
| {方案 B} | {一句理由} |

## Assumptions（假设 —— 我拍的默认值）

> 简单默认值可自主决策，写明即可。改这里要通知下游。

- {e.g. 数据库用 PostgreSQL 15}
- {e.g. API 风格 REST + JSON，错误码格式 `GS-BXX-NNNN`}
- {e.g. 鉴权用 JWT，access token 15min}

## Out of Scope（明确不做 —— YAGNI）

| 砍项 | 为什么本轮不做 |
|------|---------------|
| {e.g. 自定义短码} | {用户没提，YAGNI} |
| {e.g. 多语言} | {xdd_run+1 再加} |

## Open Questions（待用户定 —— 关键决策）

> 这些是必须用户拍板的，不自主决定。

- [ ] {e.g. 短码生成策略：自增 ID + base62 还是随机 hash？影响冲突处理}
- [ ] {e.g. 单库还是分库？影响后续扩展}
