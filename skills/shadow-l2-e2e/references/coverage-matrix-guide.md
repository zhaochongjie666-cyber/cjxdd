# 覆盖矩阵详细指南

## 目录

1. [覆盖维度详解](#覆盖维度详解)
2. [覆盖矩阵模板](#覆盖矩阵模板)
3. [P0/P1 优先级标注规则](#p0p1-优先级标注规则)
4. [四层覆盖完整性校验](#四层覆盖完整性校验)
5. [覆盖简报模板](#覆盖简报模板)

---

## 覆盖维度详解

对每条规则，覆盖矩阵系统性地枚举以下维度。

S 级项目（8 维）：仅维度 1-8。
M 级项目（10 维）：维度 1-10。
L 级项目（14 维）：全部维度。

| # | 维度 | 说明 | 列数示例 |
|---|------|------|---------|
| 1 | **主流程（Happy Path）** | 规则正常执行的所有路径变体 | 至少 1 |
| 2 | **异常路径（Error Path）** | 每条规则中每个可识别的错误条件（来自 spec.md 异常描述） | = 异常数 |
| 3 | **前置条件违反（Precondition Violation）** | 每条前置条件不满足时的行为 | = 前置条件数 |
| 4 | **权限检查（Permission/Role）** | 每个角色对该规则的行为（谁可以/谁不可以） | = 角色数 |
| 5 | **状态机转换（State Machine）** | 每条规则触发的合法状态转换 + 禁止的非法转换 | 合法+非法 |
| 6 | **边界（Boundary）** | 参数/数据的空值、最小值、最大值、超长、特殊字符 | 至少 2 |
| 7 | **副作用/领域事件（Side Effects）** | 每个领域事件的发布断言（必须发布/必须不发布/载荷校验） | = 事件数×2 |
| 8 | **数据完整性（Data Integrity）** | 操作后的数据状态变化（创建/更新/删除的数据正确性） | 至少 1 |
| 9 | **幂等性（Idempotency）** | 重复执行同一操作时的行为（如适用） | 0 或 1 |
| 10 | **并发/竞态（Concurrency）** | 并发操作时的行为（如适用） | 0 或 1 |
| 11 | **会话连续性（Session Continuity）** | 浏览器跨会话恢复、关闭标签页后重新打开、断点续作、多标签页切换、会话过期后重新登录 | 至少 1 |
| 12 | **用户误操作（User Error）** | 浏览器中误点、后退按钮、刷新页面、双击提交按钮、撤销操作、误删后恢复 | 至少 1 |
| 13 | **环境多样性（Environment）** | 不同浏览器/网络条件/屏幕尺寸/设备类型对用户操作的影响 | 0 或 1 |
| 14 | **UX 反馈点（UX Feedback）** | 每个浏览器操作的 loading/empty/error/success/notification 反馈验证 | 至少 1 |

维度 11-14 判定规则：
- 维度 11-12：对用户直接操作的规则**强制覆盖**（纯后台规则可 N/A，需说明理由）。
- 维度 13：有前端项目时**强制覆盖**（纯后端项目可 N/A）。
- 维度 14：对每个用户可感知的浏览器操作**强制覆盖**。
- 所有 N/A 必须在矩阵中写明理由，不能省略行。

## 覆盖矩阵模板

```markdown
## 覆盖矩阵

### annotation-R12：创建标注结果

| 维度 | 测试点 | 场景名 | @covers | 优先级 | 状态 |
|------|--------|--------|---------|--------|------|
| 主流程 | 标注员对文本创建有效标注 | 创建文本标注 | R12 | P0 | ✅ |
| 主流程 | 标注员对图片创建有效标注 | 创建图片标注 | R12 | P0 | ✅ |
| 异常路径 | 标签不在项目标签模板中 → 400 | 无效标签被拒绝 | R12 | P1 | ✅ |
| 异常路径 | 选区超范围 → 400 | 选区超范围 | R12 | P1 | ✅ |
| 前置条件 | 任务未打开 → 403 | 未打开任务不可标注 | R12 | P1 | ✅ |
| 前置条件 | 任务已提交 → 400 | 已提交任务不可再标注 | R12 | P1 | ✅ |
| 权限 | 标注员可创建自己任务的标注 | 标注员可标注 | R12+R11 | P0 | ✅ |
| 权限 | 审核员不能创建标注 | 审核员不可标注 | R12 | P1 | ✅ |
| 权限 | 管理员不能创建标注 | 管理员不可标注 | R12 | P1 | ✅ |
| 边界 | 空标注值 | 空值标注被拒绝 | R12 | P1 | ✅ |
| 边界 | 超长标签文本（>1000字符） | 超长标签被截断/拒绝 | R12 | P1 | ✅ |
| 副作用 | 创建后发布 AnnotationCreated | 创建后发布事件 | R12 | P1 | ✅ |
| 副作用 | 创建后不发布其他事件 | 创建后不发布无关事件 | R12 | P1 | ✅ |
| 数据完整性 | 创建后返回值包含完整数据 | 创建返回完整数据 | R12 | P1 | ✅ |
| 会话连续性 | 浏览器中创建标注途中关闭标签页，重新打开后草稿恢复 | 关闭标签页后草稿恢复 | R12 | P1 | ✅ |
| 用户误操作 | 浏览器中双击"保存标注"按钮，不创建重复标注 | 双击提交防护 | R12 | P1 | ✅ |
| UX反馈 | 点击"保存标注"后按钮变为 loading 状态，完成后显示"保存成功"toast | 保存操作浏览器反馈 | R12 | P1 | ✅ |
```

## P0/P1 优先级标注规则

**P0 判定规则**（满足任一即为 P0）：
- 核心业务路径（用户完成主要目标的最短路径上的每一步）
- 涉及金钱/数据安全/权限校验的场景
- spec.md 中标注为 P0 的规则对应的场景
- 失败后用户无法自行恢复的场景

**P1 判定规则**：
- 非核心路径的分支/边界/异常场景
- 用户体验优化类场景（如 loading 状态、toast 提示）
- 高频但不影响业务闭环的场景

优先级标注在覆盖矩阵的"优先级"列，在 UAT 剧本标题中以 `[P0]` 或 `[P1]` 前缀标注。

## 四层覆盖完整性校验

### 第一层：业务线覆盖

**来源**：`business-landscape.md` 的业务线注册表

```markdown
## 业务线覆盖

| 业务线 | 编号 | e2e.md 存在 | 真实场景数 | UAT 剧本数 | 状态 |
|--------|------|-----------|-----------|-----------|------|
| 标注管理 | B01 | ✅ | 3 | 2 | ✅ 100% |
| 项目管理 | B02 | ✅ | 2 | 1 | ⚠️ UAT不足 |
| 用户管理 | B03 | ❌ | 0 | 0 | ❌ 缺失 |
```

通过标准：每个业务线有 e2e.md，真实场景 ≥3，覆盖所有规则×维度矩阵行。

### 第二层：页面覆盖

**来源**：`wire.svg` `<metadata>` 的 pages 注册表 + `data-page`

```markdown
## 页面覆盖

| 页面 | data-page | data-route | e2e 场景覆盖 | 浏览器操作验证 | 状态 |
|------|-----------|------------|-------------|--------------|------|
| 登录页 | page-login | /login | ✅ RS-01 | ✅ fill+click | ✅ |
| 任务列表 | page-task-list | /tasks | ✅ RS-01 | ✅ click | ✅ |
| 标注工作台 | page-annotator | /tasks/:id | ✅ RS-01, RS-02 | ✅ drag+click | ✅ |
| 项目设置 | page-settings | /settings | ❌ | ❌ | ❌ 缺失 |
```

### 第三层：交互点覆盖

**来源**：`wire.svg` `<metadata>` 的 interactions + `data-action`

```markdown
## 交互点覆盖

| 交互 | data-action | data-node | data-rule | e2e 场景覆盖 | 状态 |
|------|-------------|-----------|-----------|-------------|------|
| 保存标注 | save-annotation | B01-N08 | R12 | ✅ RS-01, RS-02, RS-03 | ✅ |
| 提交标注 | submit-annotation | B01-N10 | R15 | ✅ RS-01 | ✅ |
| 导出标注 | export-annotations | B01-N15 | R22 | ❌ | ❌ 缺失 |
```

### 第四层：API 端点覆盖

**来源**：`architecture.md` §7 API 端点清单

```markdown
## API 端点覆盖

| 端点 | 方法 | @flow | @rules | e2e 场景覆盖 | 状态 |
|------|------|-------|--------|-------------|------|
| /api/annotations | POST | B01-N08 | R12 | ✅ RS-01 | ✅ |
| /api/annotations/export | GET | B01-N15 | R22 | ❌ | ❌ 缺失 |
```

### 综合覆盖率

```
综合覆盖率 = 业务线×0.25 + 页面×0.25 + 交互点×0.25 + API端点×0.25
目标 = 100%
```

## 覆盖简报模板

```markdown
## 覆盖简报

### 逐规则覆盖
| 规则 | 矩阵行数 | 场景数 | 状态 |
|------|---------|--------|------|
| R12 创建标注 | 18 | 18 | ✅ 100% |
| R15 提交标注 | 10 | 10 | ✅ 100% |

### 用户旅程覆盖
| 角色 | 画像数 | 总旅程数 | 已设计场景 | 覆盖率 |
|------|--------|---------|-----------|--------|
| 标注员 | 3 | 21 | 21/21 | 100% |

### 四层覆盖完整性
| 层级 | 覆盖率 | 缺口 |
|------|--------|------|
| 业务线覆盖 | 100% (2/2) | — |
| 页面覆盖 | 80% (4/5) | page-project-settings |
| 交互点覆盖 | 83% (5/6) | export-annotations |
| API 端点覆盖 | 80% (4/5) | GET /api/annotations/export |

### 综合覆盖度
- 规则×维度矩阵覆盖: 98%
- 综合覆盖度: 100%
- 回溯发现: 3（全部已修复）
```
