# Backend 开发约定

> 由 xdd-init 生成默认骨架。用户按项目实际情况修改。
> AI 写后端代码前必读本文件。

## 分层

按 xdd-architecture 选定的架构模式组织（不默认套 4 层）。常见：
- 分层模式：Presentation(UI+API) → Application → Domain → Infrastructure
- 六边形：Domain 核心 + Port(接口) + Adapter(技术实现)
- 事件总线/CQRS：读/写模型分离 + 事件存储

> 实际选型见 `.xdd/design/architecture/{slug}/architecture.md` §技术栈。

## 错误处理

- 统一错误响应格式：`{ "code": "STRING_CODE", "message": "人可读", "detail": {...} }`
- HTTP 状态码语义：4xx 客户端错误 / 5xx 服务端错误
- 错误码全项目统一命名（跨业务线一致），见 spec rules.md
- 所有失败路径有可读错误 + 下一步建议

## Auth / Authz

- 认证：JWT / Session / OAuth2（按 architecture.md §SDD 选型）
- 授权：RBAC / ABAC + 数据隔离
- 禁止硬编码 `current_user`，必须从认证上下文取
- 越权访问返回 403，不泄露资源是否存在

## 持久化

- 真实持久化（**禁止 InMemoryRepository / mock DB**）
- 迁移走 alembic / goose / 等价工具，版本化
- 写 → 查 → 重启后还在，是验收底线
- 事务边界明确（强一致单聚合 / 最终一致跨聚合事件）

## API 设计

- RESTful 资源路径（或按 architecture 选 gRPC）
- 版本前缀：`/api/v1/...`
- 每个端点对照 architecture 端点清单（别 60→23 的 sham）
- 限流 / 输入校验 / 输出编码 必有

## 事件

- 跨聚合/跨服务用事件驱动
- 事件契约见 `.xdd/design/architecture/event-contract.md`
- 幂等键 + 重试策略 + 死信队列

## 测试

- 每个 RXX 规则至少 1 个测试
- 代码标注 `@implements RXX`，测试标注 `@covers RXX`
- 测试跑真实依赖（DB 起来），不是全 mock
- 看断言质量，不只看 GREEN 数

## 代码纪律（反 sham）

- 无存根：`pass` / `TODO` / `return None` / `NotImplementedError` 禁止
- 无假实现：mock DB / 硬编码用户 / 假数据 禁止
- 跑通有证据：curl / 数据查询，不是"应该能跑"
- 不假完成：没跑通直说没跑通

# 代码风格
函数不得超过50行（注释+代码），尽可能压缩注释成一行
文件行数不得超过500行