# UI/UX 开发约定

> 由 xdd-init 生成。基于 xdd-wire 的 UX 审查框架，转译为开发时遵守的约定。
> 用户按项目实际情况修改（如加设计 token、组件库版本）。AI 写前端代码前必读。

## 心法

UX = 艺术 × 科学。每个交互决策回答两问：**用户感受到什么？** + **我们怎么知道它有效？**
好的 UX 不是好看的界面，而是让用户忘记界面存在 —— 达成目标时直接离开。

## 4 级约定（开发时逐级满足）

### 🔴 L1 功能性（不满足则不可用）

- 主任务 CTA 视觉显著、文字明确，用户一眼知道点哪
- 所有失败路径有可读错误 + 下一步建议
- loading / success / error 三态明确，状态可见
- 删除等破坏性操作二次确认，默认焦点在"取消"
- 所有交互 Tab 键可达，Enter/Space 触发

### 🟡 L2 可用性（不满足则难用）

- 相似功能用相似模式（组件库统一按钮/表单/弹窗行为）
- 大小/颜色/间距建 3 层视觉层次，第一眼看出谁最重要
- 单屏元素不过载，格式塔分组（接近/相似/封闭）
- 反馈即时：<100ms 瞬时；>1s 必 loading
- 误操作可挽回：Undo / Back / Exit
- 触控目标 ≥ 44×44px，间距防误触
- 文案动词开头、避免术语、说结果不说过程

### 🟢 L3 可达性 a11y（不满足则排他）

- 语义化标签：`<button>`/`<a>`/标题层级正确
- 图标按钮 `aria-label`，动态区 `aria-live`
- 颜色对比度：文字背景 ≥ 4.5:1（大字 3:1）
- 键盘聚焦有清晰 outline（**别 `outline:none`**）
- 图片有 `alt`，装饰图 `alt=""`
- label 关联 input（`<label for>`）
- 尊重 `prefers-reduced-motion`

### 🔵 L4 体验质感（情感层）

- 微交互：hover / active / disabled 有反馈
- 动效帮理解，非炫技
- 空状态友好引导，非空白
- 加载用 skeleton 优于转圈
- 成功有庆祝（toast / 动画）
- 品牌一致：配色 / 字体 / 语气统一

## 10 个高频反模式（必须规避）

1. **鬼按钮**：按钮看着像文字或链接，或反之
2. **无限滚动没尽头**：用户不知在哪、要多久
3. **模态套模态**：多层弹窗嵌套，用户迷失
4. **占位符代标签**：只用 placeholder 没 label（聚焦即消失）
5. **错误只说"失败"**：不说为什么、怎么办
6. **加载态消失但内容未到**：瞬间空白闪烁
7. **响应式只断大屏**：平板/折叠屏崩坏
8. **图标无标签**：纯图标按钮没文字或 aria-label
9. **必填项没标记**：提交后才报错，而非进入时就知道
10. **暗模式只是反色**：对比度色相未调，伤眼

## 设计 token / 组件库

> 用户在此填写项目实际选型：
>
> - 组件库：（如 Ant Design / shadcn/ui / Element Plus）
> - 设计 token：（颜色/字号/间距变量来源）
> - 图标库：
> - 动效库：
